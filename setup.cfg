[metadata]
name = shepherd-sdk
version = 0.1.0
description = Lightweight Python client for the Shep Go2 API

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.8

[options.packages.find]
where = src

[options.extras_require]
telemetry =
    websocket-client>=1.3,<2

